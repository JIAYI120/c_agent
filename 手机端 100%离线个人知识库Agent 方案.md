# 手机端 100%离线个人知识库Agent 最终落地方案（轻量版）

## 一、项目目标（你要的核心功能）

1\. 纯离线运行，**全程无网络、无后台上传、无任何联网权限**，隐私百分百本地留存。

2\. 支持手动录入个人数据：身高、体重、喜好、习惯、日程、笔记、个人偏好等。

3\. 自动构建**本地私有知识库**，后续提问自动调取个人信息作答。

4\. 打包为安卓APK，装在手机即可用，无需任何云端服务。

5\. 全部使用轻量模型，手机低配置也能流畅跑。

## 二、整体架构（100%本地离线）

整套系统分为三模块，全部运行在手机本地，无网络依赖：

**1\. 本地向量知识库（离线存储）**

替代笨重的 ChromaDB，采用手机端最稳方案：**SQLite \+ 本地余弦相似度检索**

用途：存储你所有个人信息文本 \+ 本地生成的向量，实现RAG检索。

优点：零依赖、不联网、不闪退、体积几乎为0，完美适配安卓。

**2\. 轻量离线Embedding模型（文本向量化）**

选用：**all\-MiniLM\-L6\-v2（量化轻量版）**

体积：\~30MB，极小、极速、省电。

作用：把你的个人资料、用户问题转为向量，实现智能匹配检索。

结论：**个人知识库场景，精度完全够用，无任何替代压力**。

**3\. 轻量离线大模型（问答推理）**

最终选型（最优手机平衡）：**Qwen2\.5\-0\.5B\-Instruct Q4\_K\_M GGUF**

体积：300MB左右

运行门槛：4G内存及以上所有安卓手机全覆盖

能力：精准根据知识库内容回答、不会乱编、适合个人资料问答、偏好推荐、信息整理。

**核心优势：RAG加持下，小模型完全够用，体验接近大模型**

## 三、完整离线工作流程（断网/飞行模式可用）

1\. 用户在APP内输入个人信息（身高、喜好、作息、目标等）

2\. 本地Embedding模型生成向量，存入本地SQLite知识库

3\. 用户提问 → 问题本地向量化

4\. 本地检索相似度最高的个人资料（1\-3条）

5\. 自动拼接固定约束Prompt（防止幻觉）

6\. 本地Qwen轻量模型离线推理生成答案

7\. 展示结果，**全程0网络请求、0数据上传**

## 四、技术选型（稳、轻、可落地、适合手机）

**开发框架：Flutter**

理由：跨端、UI快速、打包简单、适配安卓所有机型，比原生Android开发快3倍。

**推理引擎：llama\.cpp ARM64 离线编译库**

行业移动端离线模型标准方案，性能最强、功耗最低。

**数据库：SQLite（APP私有目录）**

永久本地存储，卸载APP才会删除数据。

**网络权限：彻底关闭**

AndroidManifest 移除所有网络权限，系统级禁止联网，真正纯离线。

## 五、APK成品形态（最终你拿到的东西）

1\. 一个**十几MB的纯净APK**

2\. 一份**300MB Qwen0\.5B离线模型文件**（手动放入手机指定文件夹）

3\. 安装后飞行模式完全可用

4\. 所有个人数据只存在你手机本地，任何人无法获取

## 六、核心功能清单（最终版）

✅ 个人信息手动录入、修改、删除

✅ 自动构建本地私有知识库

✅ 离线智能检索相关个人资料

✅ 基于个人信息离线问答

✅ 无广告、无联网、无后台、无隐私泄露

✅ 轻量低功耗，手机不发烫、不卡顿

## 七、为什么轻量模型**绝对够用**（核心论证）

你的场景**不是通用AI，是个人资料专属问答**：

1\. 不需要模型自带世界知识

2\. 所有答案素材全部由本地RAG检索提供

3\. 模型只做「阅读理解\+整理回答」极简任务

4\. 固定约束Prompt可彻底杜绝小模型幻觉

**行业共识：RAG场景下，0\.5B轻量模型完胜裸跑7B大模型**

## 八、设备适配门槛（极低）

✅ 安卓 9\.0 以上全部机型

✅ 4GB 内存即可流畅运行

✅ 只需预留 500MB 手机存储

## 九、固定防幻觉 Prompt（内置到代码）

严格约束轻量模型，保证回答精准、不乱编：

你是用户的专属离线私人助手，**只能根据下方【个人知识库】内容回答问题**。

1\. 知识库没有的信息，一律回答：暂无相关个人资料

2\. 禁止编造、推测、联想任何用户信息

3\. 回答简洁、贴合用户个人情况

【个人知识库】

\{\{retrieve\_data\}\}

用户问题：\{\{question\}\}

## 十、方案总结（最简一句话）

**用最轻量、最稳、最隐私的手机离线RAG架构，实现你的专属私人知识库AI，全程断网可用，完全满足个人日常问答需求。**

> （注：部分内容可能由 AI 生成）
